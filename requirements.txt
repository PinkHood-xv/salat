# Core dependencies
python-dateutil>=2.8.0

# Optional dependencies for specific log formats
# Uncomment the ones you need:

# For Windows Event Log (EVTX) parsing
# python-evtx>=0.7.0

# For advanced JSON processing (if needed)
# jsonschema>=4.0.0

# For CSV output formatting
# pandas>=1.3.0

# Note: PCAP analysis requires tshark/wireshark to be installed on the system
# Install on Debian/Ubuntu: sudo apt-get install tshark
# Install on macOS: brew install wireshark
# Install on RHEL/CentOS: sudo yum install wireshark
